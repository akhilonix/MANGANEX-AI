import { useState } from 'react';
import { ArrowDownRight, ArrowRight, RefreshCw, ShieldCheck } from 'lucide-react';
import { Link, useLocation } from 'wouter';
import { Logo } from '@/components/brand-logo';
import { adminLogin } from '@/services/adminService';

export function AdminLoginPage() { const [, setLocation] = useLocation(); const [busy, setBusy] = useState(false); const [error, setError] = useState(''); const [email, setEmail] = useState('admin@manganex.ai'); const [password, setPassword] = useState('demo-admin'); const enterAdmin = async () => { if (!email.trim() || !password.trim()) return; setBusy(true); setError(''); try { await adminLogin(email.trim(), password); window.localStorage.setItem('manganex-admin-session', 'demo'); setLocation('/admin'); } catch { setError('Invalid admin email or access code.'); } finally { setBusy(false); } }; return <div className="ops-grid flex min-h-[100dvh] items-center justify-center bg-[hsl(var(--background))] p-5"><div className="w-full max-w-[520px] border border-[hsl(var(--border))] bg-[hsl(var(--card)/.96)] shadow-[0_25px_80px_hsl(207_35%_16%/.18)]"><div className="border-b border-[hsl(var(--border))] bg-[hsl(var(--sidebar))] p-7 text-[hsl(var(--sidebar-foreground))] md:p-9"><Logo /><div className="mt-12 flex items-start gap-4"><span className="grid h-11 w-11 shrink-0 place-items-center border border-[hsl(var(--primary)/.45)] bg-[hsl(var(--primary)/.13)] text-[hsl(var(--primary))]"><ShieldCheck size={21} /></span><div><p className="eyebrow text-[hsl(var(--primary))]">Elevated access</p><h1 className="mt-2 font-display text-5xl leading-none">ADMIN PANEL</h1><p className="mt-3 max-w-sm text-[11px] leading-5 text-[hsl(var(--sidebar-foreground)/.62)]">Manage your team identity, members and command-center controls.</p></div></div></div><div className="p-7 md:p-9"><div className="mb-6 border border-[hsl(var(--primary)/.24)] bg-[hsl(var(--primary)/.07)] p-3 font-mono-ui text-[9px] leading-4 text-[hsl(var(--muted-foreground))]">Demo admin access is enabled for the evaluation workspace. No production credentials are used.</div><div className="space-y-4"><label className="block"><span className="eyebrow mb-2 block">Admin email</span><input data-testid="input-admin-email" value={email} onChange={(event) => setEmail(event.target.value)} type="email" className="w-full border border-[hsl(var(--border))] bg-transparent px-3 py-3 text-[12px] outline-none focus:border-[hsl(var(--primary))]" /></label><label className="block"><span className="eyebrow mb-2 block">Access code</span><input data-testid="input-admin-password" value={password} onChange={(event) => setPassword(event.target.value)} type="password" className="w-full border border-[hsl(var(--border))] bg-transparent px-3 py-3 text-[12px] outline-none focus:border-[hsl(var(--primary))]" /></label></div>{error && <p role="alert" className="mt-4 border border-[hsl(var(--accent)/.35)] bg-[hsl(var(--accent)/.08)] px-3 py-2 font-mono-ui text-[9px] text-[hsl(var(--accent))]">{error}</p>}<button onClick={enterAdmin} disabled={busy || !email.trim() || !password.trim()} data-testid="button-enter-admin" className="mt-6 flex w-full items-center justify-between bg-[hsl(var(--primary))] px-4 py-3.5 text-[11px] font-bold text-[hsl(var(--primary-foreground))] hover:brightness-95 disabled:cursor-not-allowed disabled:opacity-50">{busy ? 'Opening admin panel…' : 'Enter admin panel'}{busy ? <RefreshCw size={15} className="animate-spin" /> : <ArrowRight size={15} />}</button><Link href="/login" data-testid="link-back-to-demo-login" className="mt-5 inline-flex items-center gap-2 font-mono-ui text-[9px] uppercase tracking-[.1em] text-[hsl(var(--muted-foreground))] hover:text-[hsl(var(--foreground))]"><ArrowDownRight size={13} className="rotate-45" /> Back to demo login</Link></div></div></div>; }
